export const DEMO_DOCUMENT = `
GridOne AI is a collaborative intelligence network designed to let ordinary people participate in AI infrastructure.
Builders choose when and how their computers contribute idle capacity. Smart Contribution keeps foreground activity in
control and pauses work when device conditions cross the Builder's limits. The coordinator divides public or synthetic
documents into bounded microtasks. Independent worker processes transform each chunk into a deterministic feature-hash
vector. Results are never trusted merely because a worker returned them. The verifier recomputes the expected receipt,
checks the input digest, token count, vector dimensions, and numerical values, then rejects or accepts the result.
Only verified work creates prototype AI Credits. Every credit entry links to a receipt that identifies the task, worker,
input digest, useful work units, and verification time. Prototype credits are explicitly non-redeemable and have no cash
value. The Build Week demonstration uses separate worker processes, synthetic content, automatic task leases, retry after
a Builder pauses, and one intentionally tampered result so judges can see that invalid work is rejected. GridOne does not
claim to replace data centers or train frontier models over consumer internet connections. It demonstrates a smaller and
more credible idea: a community-powered layer for divisible, latency-tolerant, independently verifiable AI workloads.
Developers gain another way to execute supported batch tasks. Builders gain agency, transparency, and a visible record of
their contribution. The long-term vision is an AI compute cooperative in which value flows back to the people who help
create it. Don't just use AI. Help build it.
`;

